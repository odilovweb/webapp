import { createSlice } from "@reduxjs/toolkit";
const initialState = {};

const solanaXi = createSlice({
  name: "comfy",
  initialState,
  reducers: {},
});
export const {} = solanaXi.actions;

export default solanaXi.reducer;
